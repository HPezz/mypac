# Relevant issue comments

Reporter confirms the failure is deterministic on Node 22 with `isEven(3)`.
No PRD or ADR is linked. The focused implementation is `is-even.mjs`; the focused reproduction is `is-even.test.mjs`.
